{
    "version": 2,
    "name": "useronboarding",
    "builds": [{ "src": "useronboarding/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "useronboarding/$1" }
    ]
}
